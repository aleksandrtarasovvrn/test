pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html public/
    - cp style.css public/
    - cp script.js public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
